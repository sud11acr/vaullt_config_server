server:
  port: 8080
spring:
  data:
    mongodb:
      database: customer
      host: localhost
      port: 27017
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8098/eureka/
