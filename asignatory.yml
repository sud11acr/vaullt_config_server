server:
  port: 8088
spring:
  data:
    mongodb:
      database: authorizedsignatories
      host: localhost
      port: 27017
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8098/eureka/